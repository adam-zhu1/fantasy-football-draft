# environment
.venv/
__pycache__/
*.pyc
.DS_Store

# FantasyPros exports are their data, not ours to redistribute; nflverse cache is re-downloadable
data/*.csv
!data/flags.csv
data/cache/

# live draft state and generated outputs
data/draft_state.json
board.csv
board.txt
data/league_rosters.json
data/league_schedule.json
week*_report.md
data/week_lineups.json

# ESPN session cookies (espn_s2 / SWID) — login credentials, never commit
data/espn_auth.json

# built dashboard: carries real names and rosters, same as league_rosters.json
dashboard.html
data/dashboard_build.log
